const config = {
    // basename: stating the build configure name
    basename: '/Quic!',
    defaultPath: '/',
    fontFamily: `'Roboto', sans-serif`,
    borderRadius: 12
};

export default config;
